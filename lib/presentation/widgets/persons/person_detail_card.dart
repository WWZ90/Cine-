import 'package:flutter/material.dart';

class PersonDetailCard extends StatelessWidget {
  const PersonDetailCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}